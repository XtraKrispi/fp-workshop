{
  "name": "videogamedb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "elm": "elm-app start",
    "json": "json-server --watch db.json --routes routes.json --port 1234 --delay 1000",
    "start": "concurrently --kill-others \"npm run elm\" \"npm run json\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^3.5.1",
    "create-elm-app": "^1.10.3",
    "json-server": "^0.12.1"
  }
}
